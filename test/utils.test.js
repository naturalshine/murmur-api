import { uploadToWeb3Storage } from '../src/utils/storeMedia';
import { staticPath } from '../src/settings';
import { expect } from './setup';

describe('UTILS', () => {
    /*
    it('uploads to web3.storage', async() => {
        const files = ['/home/cst/code/murmur/murmur-api/src/resources/static/assets/uploads/deer.png'];
        const res = await uploadToWeb3Storage(files);
        expect(res.success).to.equal(true);
    }).timeout(10000);

    */


});